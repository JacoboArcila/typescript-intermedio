const numbers: ReadonlyArray<number> = [1,2,3,4];

/* numbers.push(1); */
/* numbers.pop(); */

//lo inserta al inicio del array
/* numbers.unshift(2); */

numbers.filter(() => {})
numbers.reduce(() => 0)
numbers.map(() => {})
